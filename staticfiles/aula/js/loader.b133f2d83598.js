document.addEventListener("DOMContentLoaded", function() {
    // Oculta el loader una vez que la página haya cargado
    const loader = document.getElementById("loader");
    loader.classList.add("hidden");
});